---
description: "[DEPRECATED] Use /cross-team instead"
allowed-tools: []
---

> **⚠ DEPRECATED:** `/cross` has been replaced by `/cross-team`, which handles both single-task and multi-task execution.

Run `/gsd-vgl:cross-team` instead. It will automatically detect whether one or multiple tasks are unblocked and execute accordingly.
